// Define the User interface to match the expected structure of user data

export interface User {
    id: number;
    firstName: string;
    email: string;
    lastName: string;
    gender: string;
    image: string;
}